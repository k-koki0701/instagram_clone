<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @blog, local: true) do |form| %>
  <%= image_tag(@blog.picture.url) if @blog.picture && @blog.picture.url %>
  <%= form.hidden_field :picture_cache %>
  <%= form.hidden_field :content %>
  <p>本文:<%= @blog.content %></p>
  <%= form.submit  "登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
